services:
  - type: web
    name: attendance-system
    env: docker
    plan: free
    dockerfilePath: ./Dockerfile
    region: oregon
    buildCommand: docker build -t attendance-system .
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHONUNBUFFERED
        value: 1

